---
allowed-tools: Bash(python3:*)
description: Configure AI routing and providers (interactive)
---

## Configuration

- !`python3 ${CLAUDE_PLUGIN_ROOT}/scripts/ai_config.py config`

If you don't see the interactive prompt, run the command above directly in your terminal.
